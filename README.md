Main goal is to have one google sheet that hosts Odds, Live Scores, Schedule,
and maps all schools across them.

Then each participant has their own google sheet that pulls data from main sheet using importrange
People can place bets in their spreadsheet (timestamped to verify done before tip-offs).

Each participant's results then feed back to main sheet (or a 3rd sheet?) for tracking
and shit-talking.
